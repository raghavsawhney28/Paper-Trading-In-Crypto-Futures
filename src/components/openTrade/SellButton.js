import classes from "./SellButton.module.css";

const SellButton = () => {
  return (
    <div>
      <button className={classes.sell_button}>Sell </button>
    </div>
  );
};

export default SellButton;
